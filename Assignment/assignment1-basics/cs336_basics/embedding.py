import torch
import torch.nn as nn

class Embedding(nn.Module):
    def __init__(self, num_embeddings, enbedding_dim, device=None, dtype=None):
        pass
        
    
    def forward(self, x: torch.Tensor) -> torch.Tensor:
        pass